const LOCALHOST = 'localhost'
const ROOT = 'root'
const PASSWORD = 'Davemiu@132127'
const DATABASE = 'entries'
const DB_SELECT = 'SELECT * FROM entries WHERE word ='

module.exports = {
    host: LOCALHOST,
    dbUsername: ROOT,
    dbPassword: PASSWORD,
    db: DATABASE,
    dbSelect: DB_SELECT
}
